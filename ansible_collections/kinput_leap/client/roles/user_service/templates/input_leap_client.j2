[Unit]
Description=Input Leap Client
After=network.target

[Service]
ExecStart=input-leapc -f {{ input_leap_server_ip }}
Restart=always
{% for key, value in environment_variables.items() %}
Environment="{{ key }}={{ value }}"
{% endfor %}

[Install]
WantedBy=default.target
